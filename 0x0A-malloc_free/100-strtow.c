#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * **strtow - return a pointer 2D  array.
 * @str: char
 * Return: pointer to pointer.
 */
char **strtow(char *str)
{
	int i, j;
	int **cadena = 0;

	if (str == 0 || str == "")
		return (NULL);

	for (i = 0; i < srt; i++)
	{}
	for (j = 0; j < srt; j++)
	{}
	return (cadena);
}
