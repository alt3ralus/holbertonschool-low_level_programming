#include "holberton.h"

/**
 * _strncat - A function that concatenates two string.
 * @dest: char type
 * @src: char type
 * @n: integer type.
 * Return: A char type
 */

char *_strncat(char *dest, char *src, int n)
{
	int i, j;

	for(i = 0; dest[i] != '\n'; i++)
	{
	}

	for(j = 0; n > 0 &&  src[j] != '\n'; j++)
	{
	}

	return (dest);
}
