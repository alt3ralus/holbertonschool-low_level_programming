#include "lists.h"
/**
 * add_dnodeint - adds not to start of dlinked list
 * @head: double ointer to list
 * @n: an int.
 * Return: the address of the new element, or NULL if it failed.
 */
dlistint_t *add_dnodeint(dlistint_t **head, const int n)
