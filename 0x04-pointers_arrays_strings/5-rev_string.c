#include "holberton.h"
#include <stdio.h>
/**
 * rev_string - It Reverses a string.
 * @s: *char
 * Return: Void.
 */
void rev_string(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	s[0] = s[i-1];

/*	while(i == 0)
	{
		putchar(s[i]);
		i--;
		}*/

}
